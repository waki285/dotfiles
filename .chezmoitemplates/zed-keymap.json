[
  {
    "context": "Terminal",
    "bindings": {
      "shift-enter": [
        "terminal::SendText",
        "\u001b\r"
{{- if eq .chezmoi.os "windows" }}
      ],
      "ctrl-w": ["pane::CloseActiveItem", { "close_pinned": false }],
      "shift-up": null
{{- else }}
      ]
{{- end }}
    }
  },
  {
    "context": "Editor",
    "bindings": {
      "ctrl-t": null
    }
  }
]
